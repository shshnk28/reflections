how did viewing a diff between two versions of a file help you see the bug that was introduced?
we can see the text difference to pinpoint what has gone wrong.

How could having easy access to the entire history of a file make you a more efficient programmer in the long term?
you can see what has changed across the files.
?

What do you think are the pros and cons of manually choosing when to create a commit, like you do in Git, 
vs having versions automatically saved, like Google docs does?
we get to choose when to save a version.

Why do you think some version control systems, like Git, allow saving multiple files in one commit, 
while others, like Google Docs, treat each file separately?

so the state of the project can be tracker as a whole

How can you use the commands git log and git diff to view the history of files?

git log gives the entire history and diff gives the diference betweene two commits 

How might using version control make you more confident to make changes that could break something?

we know that we can revert back to a stable version anytime

Now that you have your workspace set up, what do you want to try using Git for?

to mees up the code
